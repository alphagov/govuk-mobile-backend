Feature:User Pool Identity Provider
    Scenario: A template can deploy theUser Pool Identity Provider
        Given a template to deploy theUser Pool Identity Provider
        Then the template must have the required resource and properties to deploy theUser Pool Identity Provider
Feature: GOV UK User Pool Identity Provider
    Scenario: A template can deploy the GOV UK User Pool Identity Provider
        Given a template to deploy the GOV UK User Pool Identity Provider
        Then the template must have the required resource and properties to deploy the GOV UK User Pool Identity Provider
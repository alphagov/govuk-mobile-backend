Feature: Pre Authentication Lambda
    Scenario:  A template can deploy Attestation Lambda
        Given a template to deploy the GOV Attestation Lambda
        Then the template must have the required resource and properties to deploy Attestation Lambda
{
  "name": "pager-duty-test-endpoint",
  "version": "1.0.0",
  "description": "Pager Duty Test Endpoint",
  "main": "app.js",
  "author": "govuk-app-backend",
  "license": "MIT",
  "scripts": {
    "unit": "vitest run",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "build:debug": "esbuild --bundle app.ts --outfile=debug/app.js --sourcemap --platform=node --target=node22 --minify",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "esbuild": "^0.14.14"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "^20.5.7",
    "prettier": "^2.8.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4",
    "vitest": "^3.1.1"
  }
}
